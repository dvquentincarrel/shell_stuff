{
    "dependencies": ["picom"],
    "installation": {
        "config": {
            "dir": "$HOME/.config/picom",
            "files": ["picom.conf"]
        },
        "script": {
            "dir": "$HOME/.local/bin",
            "strip_ext": true,
            "files": ["tpicom.sh"]
        }
    }
}
