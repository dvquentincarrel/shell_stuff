dependencies:
- rg
- awk
- fzf
- bat
installation:
  config:
    dir: $HOME/.config/ripgrep
    files:
    - ripgrep.rc
  setup:
    dir: $HOME/.config/bash/setup
    renamed_files:
    - dest: 011_ripgrep.bash
      src: ripgrep.bash

