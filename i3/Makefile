install:
	@mkdir -p $(conf_base)/i3
	@mkdir -p $(conf_base)/dunst
	@ln -s -i $$PWD/i3_config $(conf_base)/i3/config
	@ln -s -i $$PWD/dunstrc $(conf_base)/dunst/dunstrc
	@ln -s -i $$PWD/picom.conf $(conf_base)/picom.conf
	@ln -s -i $$PWD/tpicom.sh $(conf_base)/tpicom
